KINGDOM PROPEL — CORRECT PLACEMENT + BLUEPRINT FOOTER FIX

The previous upload placed the Blueprint files at the repository root. This package
corrects both problems in one commit:

1. It restores the front-facing website at kingdompropel.com.
2. It installs the footer repair inside the real demo-blueprint folder.

UPLOAD INSTRUCTIONS

1. Unzip this download.

2. Open the folder named:
   DRAG-THESE-2-ITEMS-TO-GITHUB-ROOT

   You will see exactly two items:
   - index.html
   - demo-blueprint (a folder)

3. Open the kingdompropel repository on GitHub and stay at the repository root.
   The breadcrumb must read only:
   eaglevisiondigital / kingdompropel

4. Click Add file, then Upload files.

5. From the folder on your Mac, select and drag BOTH of these items into GitHub:
   - the index.html file
   - the entire CLOSED demo-blueprint folder

   Do not open demo-blueprint and drag out its individual files.

6. Before committing, GitHub's upload list must show these three destinations:
   index.html
   demo-blueprint/index.html
   demo-blueprint/blueprint.css

   If it shows blueprint.css by itself at the repository root, CANCEL the upload.

7. Commit with this message:
   Restore homepage and correctly install Blueprint footer fix

8. Wait for Netlify to deploy. Then check both:
   https://kingdompropel.com/
   https://kingdompropel.com/demo-blueprint/

The corrected Blueprint stylesheet uses a new version number so the browser will
not reuse the broken cached footer.
