# Propel Digital Ministry Blueprint — V58 Change Files

This change-files-only update sits on top of V57 and includes every change approved since that download:

1. Restores the two approved transparent logo assets required by **The Future** page:
   - `assets/propel-logo-lightpage-approved.png`
   - `assets/foundations/foundations-horizontal-transparent-approved.png`
2. Replaces the prior generic Foundations mobile website phone construction on **Mobile Website (07)** with the newly approved **Champion Life Church mobile website on an iPhone 17 Pro Max with black trim**.
3. Adds presentation-specific sizing/shadow treatment for that approved mobile mockup.
4. Bumps CSS/JS asset cache keys to **v58** so the browser does not retain stale V57 styling.

Replace the included files at the same paths in your Kingdom Propel project, then hard-refresh `/demo-blueprint` once.
